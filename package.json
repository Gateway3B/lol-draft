{
    "scripts": {
        "install:all": "cargo install trunk && rustup target add wasm32-unknown-unknown && cargo install cargo-leptos && ",
        "run": "cargo leptos watch",
        "seaorm:create-migration": "sea-orm-cli migrate --",
        "docker:build": "docker build -t hl-docker-registry.g3tech.net/lol-draft:latest .",
        "docker:push": "docker push hl-docker-registry.g3tech.net/lol-draft:latest"
    },
    "devDependencies": {
        "tailwindcss": "^3.4.10"
    }
}
